//TODO

/**
 * Fires when the user changes the wind speed via the GUI.
 * @param p
 */
export function onChangeWindSpeed(p) {}

/**
 * Fires when the user changes the wind direction via the GUI.
 * @param p
 */
export function onChangeWindDirection(p) {}

/**
 * Fires when the user changes the wind direction via the GUI.
 * @param p
 */
export function onChangeSunPosition(p) {}